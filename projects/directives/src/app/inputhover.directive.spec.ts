import { InputhoverDirective } from './inputhover.directive';

describe('InputhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new InputhoverDirective();
    expect(directive).toBeTruthy();
  });
});
